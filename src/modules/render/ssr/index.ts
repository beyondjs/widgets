/**
 * The server implementation of `@beyond-js/widgets/render`: the registry of widget specifications without
 * browser construction, and the type aliases the controllers share across platforms
 */
export { widgets } from './widgets';
export type { IWidgetSpecs, StylesManager, BeyondWidget } from './widgets';
