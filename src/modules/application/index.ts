/**
 * The startup of an application authored for the Engine: importing this module loads its configuration
 * and its main layout. Widgets that run on the development runtime do not use it.
 */
import './startup';
